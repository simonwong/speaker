# Speaker 隐私说明

更新日期：2026-09-22

Speaker 是一个由用户自行配置服务凭据的 macOS 语音输入工具。它不包含广告、行为分析或自动上传的崩溃报告。

## 音频

- 只有在用户主动开始录音后，Speaker 才访问麦克风。
- 音频在内存中转换为单声道 PCM，只发送到用户选中的语音识别服务：豆包（火山引擎）、OpenAI 或阿里千问。采集格式为 16 kHz，OpenAI 实时接口使用内存重采样后的 24 kHz 音频。豆包边录边传；OpenAI 和千问由用户选择边录边传，或在录音结束后发送完整音频。
- Speaker 不把原始音频写入磁盘、会话历史或剪贴板；取消录音会终止本次发送并释放内存缓冲。
- 少于 300 ms 的录音和近乎数字静音会在结束录音时由本机终止，不会进入文字送达。流式识别的音频可能在判定前已经发送，Speaker 会取消仍在进行的请求，但无法撤回服务商已接收的音频。整段识别需通过本机录音有效性检查后才上传。
- 音频到达所选服务后的处理适用用户与该服务商之间的账号、套餐和数据处理条款。千问的北京和新加坡区域由用户显式选择，不自动跨区。

## 转录与文本整理

- Speaker 会把已启用的个人词库词条作为识别提示随音频请求发送给选中的语音识别服务，以提高专有名词识别率。
- 默认“语义顺滑”只使用所选语音识别服务返回的文字，不调用文字整理服务。
- 流式识别的中间文字不写入目标应用或会话历史；录音结束后只使用最终确认的文字。
- 只有用户明确选择“精简清理”“完整重写”或自定义模式时，Speaker 才把识别服务返回的文字、所选整理规则和本次按下快捷键时的个人词库词条发送到用户所选的文字整理服务（DeepSeek、OpenAI、Kimi、GLM 或自定义 API）；词条只作为纯文本数据，用于把听错的片段纠正回用户的拼写。
- Speaker 不向 文字整理服务 发送目标应用信息或会话历史。
- Speaker 不把音频发送给 文字整理服务。
- 语音识别与文字整理请求使用临时网络会话，不持久化 Cookie、HTTP 缓存或系统 URL 凭据。

- 自定义 API 只接受 HTTPS 基础地址；更换地址后必须重新保存 Key。文字整理请求拒绝重定向，不自动切换服务。
- 豆包“检查连接”仅在用户点击后发送合成静音，文字整理检查发送合成测试文字，均可能产生服务费用；不发送真实录音或历史。OpenAI 和千问识别配置不自动探测，首次实际录音才验证可用性。

## 本地保存的数据

会话历史保存在当前 macOS 用户的 `~/Library/Application Support/Speaker/history.sqlite3`，可能包含：

- 语音转录、可选的文字整理结果和最终文字；
- 处理阶段、取消发生的阶段、耗时和服务请求 ID；
- 当次识别和整理服务、模型 ID、整理规则及个人词库快照；
- 结构化且限制范围的错误代码与内容无关的送达诊断码，不保存服务商返回的自由文本错误消息。

历史不包含原始音频、目标应用名称或其他目标身份、API Key、辅助功能对象、目标输入框原内容或剪贴板历史。目标应用身份只是送达过程中的临时诊断，既不展示也不写入会话记录。安全输入框中的结果不会写入会话历史。

新安装默认不按日期自动清理，直到用户明确选择保留最近 30 天、90 天或一年。任何设置都最多保留 10000 条，防止常驻 App 无限占用本地空间。用户可以删除单条记录或清空全部历史。

设置和个人词库同样只保存在当前用户的 Application Support 目录。目录权限为 `0700`，敏感本地文件及 SQLite sidecar 权限为 `0600`。Speaker 会在解码前重新验证并收紧这些权限；若系统拒绝修改权限，则停止加载对应文件并显示诊断。

旧版本曾创建用于豆包请求的本机安装标识；当前版本改为每次请求生成临时标识，并在启动时删除旧值。

## API Key

- 本机 ad-hoc 开发构建没有稳定签名身份，因此把 Key 保存在仅当前用户可读写的 `credentials.json`；这种构建不用于对外分发。
- 稳定 Developer ID 发布构建使用 macOS Keychain，访问级别为 `AfterFirstUnlockThisDeviceOnly`。
- 从开发构建升级到正式构建时，Speaker 只有在 Keychain 写入后读回一致，才删除旧凭据。清理失败会保留诊断并重试，不会把失败伪装成迁移成功。

- 识别与文字整理的 Key 独立保存；OpenAI 的两种用途也不自动共用 Key。千问北京和新加坡区域分别保存 Key；删除一个不会删除其他凭据。

## 麦克风选择与本地测试

- 默认跟随系统输入，也可固定选择麦克风。Speaker 不修改系统默认输入；固定设备失联时提示并停止，不自动换到另一支麦克风。
- 固定选择的设备 UID 只写入仅当前用户可访问的 `settings.json`。设备名称只用于即时界面；设备名称和 UID 不写入会话记录、识别请求或脱敏诊断。
- 设置中的电平测试只在用户点击后开始，最长 8 秒。音频仅在内存中用于计算电平，随即丢弃，不连接识别服务、不保存音频或会话记录。开始正常语音输入会结束电平测试。

## 系统权限与文字送达

- 麦克风权限仅用于录音。
- 辅助功能权限用于监听用户设置的全局快捷键、识别并验证当前输入位置，以及在目标允许读取时核实送达结果。
- 自动送达会在内存中暂存剪贴板原内容，临时写入转录文字并发送一次 Command-V。仅当剪贴板仍属于本次事务时才尝试恢复原内容；用户之后复制的新内容不会被恢复操作覆盖。剪贴板原内容不会写入磁盘。
- 剪贴板项目读取失败或无法完整暂存时，Speaker 在修改前停止操作。合法的空剪贴板仍可使用。用户主动点击“复制”并经系统读回验证成功后，新文字保留在剪贴板中，待复制结果关闭。
- 粘贴提交前，若无法确认目标安全、输入位置发生变化或目标是安全输入框，Speaker 会保留结果等待用户复制，不强制切换焦点。粘贴命令发出后，即使无法核实目标中的文字，也不会自动重试或提示可安全重复粘贴。

## 诊断信息

“复制脱敏诊断信息”只包含 App/系统版本、权限状态、快捷键、处理状态、provider 是否已配置、资源类型、整理服务、内置目录中的模型 ID 和历史存储状态。自定义地址和手填模型 ID 不进入脱敏诊断。它不包含转录文字、音频、API Key 或完整本地文件内容。用户决定是否把诊断信息提供给支持方。

## 删除与卸载

用户可以在 App 内删除 API Key、词库条目和会话历史。彻底移除本地数据时，退出 Speaker 后删除：

```text
~/Library/Application Support/Speaker
~/Library/Application Support/com.local.speaker
~/Library/Caches/Speaker
~/Library/Caches/<Speaker 的正式 Bundle ID>
~/Library/Caches/com.local.speaker
~/Library/Saved Application State/<Speaker 的正式 Bundle ID>.savedState
~/Library/Saved Application State/com.local.speaker.savedState
~/Library/Preferences/<Speaker 的正式 Bundle ID>.plist
~/Library/Preferences/com.local.speaker.plist
```

App 内的“清除本地数据并退出”会按上述范围清理并逐项验证，同时注销登录项及删除当前/旧版 Keychain 凭据；任何步骤失败都不会报告成功。手动卸载时，还应在“系统设置 → 通用 → 登录项”中移除 Speaker，并按需在“隐私与安全性”中移除麦克风和辅助功能授权。仅删除 App 本身不会自动删除用户明确保留的历史、偏好或 Keychain 凭据。

## 变更与反馈

若数据流、第三方服务或默认保留策略发生实质变化，本说明应随 App 版本一并更新。问题反馈请使用获得 Speaker 的分发渠道，并且不要发送 API Key。
