#!/bin/zsh

set -euo pipefail

SCRIPT_DIR="${0:A:h}"
CONFIGURATION="${SPEAKER_CONFIGURATION:-debug}"
exec "$SCRIPT_DIR/swiftw" build --disable-sandbox --configuration "$CONFIGURATION" --product SpeakerApp
